import * as Drei from "@react-three/drei"
import { Perf } from "r3f-perf"

export const perfImpl = Perf
export const statsImpl = Drei.Stats
export const htmlImpl = Drei.Html
export const textImpl = Drei.Text
export const outlinesImpl = Drei.Outlines
export const edgesImpl = Drei.Edges
